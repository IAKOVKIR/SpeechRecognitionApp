<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".delivery_user.delivery_user_home.DeliveryUserHomeFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/home_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/home_delivery_user"
            android:textColor="#000"
            android:textSize="20sp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/microphone_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/microphone_icon"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_baseline_mic_24"
            android:padding="8dp"
            android:tint="#FFFFFF"
            android:background="@drawable/round_square_corner"
            app:layout_constraintStart_toEndOf="@id/view_delivery_list_button"
            app:layout_constraintBottom_toBottomOf="@id/view_delivery_list_button"
            app:layout_constraintTop_toTopOf="@+id/view_delivery_list_button" />

        <Button
            android:id="@+id/view_delivery_list_button"
            android:layout_marginTop="4dp"
            android:text="@string/delivery_list"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/home_title"
            style="@style/BlueButtonStyle"/>

        <Button
            android:id="@+id/log_out_button"
            android:layout_marginTop="4dp"
            android:text="@string/log_out"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/view_delivery_list_button"
            style="@style/BlueButtonStyle"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>